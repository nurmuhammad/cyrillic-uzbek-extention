# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Лойиҳа ҳақида

Chrome кенгайтмаси (Manifest V3, vanilla JS, build қадами йўқ). Очиқ турган
саҳифанинг асосий ўқиладиган қисмини **кирилл ўзбекчага** ўгиради ва саҳифа
хулосасини тайёрлайди. Учта амал бор, учаласини ҳам фойдаланувчи ўзи танлайди -
автоматик режим танлаш йўқ:

| Амал | Режим | LLM | Изоҳ |
|---|---|---|---|
| LLM билан таржима | `llm` | керак | Ҳар қандай тилдан |
| Транслитерация | `translit` | керак эмас | Фақат лотин ўзбекча → кирилл, офлайн |
| Хулоса | `summary` | керак | Натижа саҳифадаги панелда |

## Ишга тушириш ва текшириш

Build, npm, тест фреймворки йўқ - жилднинг ўзи кенгайтма.

```bash
# chrome://extensions -> Developer mode -> "Load unpacked" -> шу жилдни танланг
```

Ўзгартиргандан кейин: `chrome://extensions` да кенгайтма картасидаги
**Reload** тугмаси. Content script ўзгарса, синаётган саҳифани ҳам янгиланг -
эски нусхаси хотирада қолиб кетади.

Дебаг қилиш жойлари уч хил, уларни адаштирманг:

- **background.js** → `chrome://extensions` → «service worker» ҳаволаси
- **content/*.js** → синалаётган саҳифанинг DevTools консоли
- **popup / options** → ойнанинг ўзида ўнг тугма → Inspect

Иконкаларни қайта яратиш (`icons/proposals/*.svg` - вариантлар, `icons/icon*.png` - ишлатилаётгани):

```bash
powershell -File tools/build-icons.ps1
```

Тарқатишга тайёр нусхани `ready/` га йиғиш (`-Zip` билан `ready.zip` ҳам):

```bash
powershell -File tools/build.ps1
```

`ready/` - генерация натижаси, унда ҳеч нарса таҳрирланмайди. Скрипт уни
ўчиришдан олдин ичида `manifest.json` борлигини текширади, шунинг учун нотўғри
жилдни кўрсатиб қўйсангиз ўчириб юбормайди.

> **PowerShell файллари UTF-8 BOM билан сақланиши шарт.** Windows PowerShell 5.1
> BOM'сиз `.ps1` ни ANSI деб ўқийди - кирилл ҳарфлар изоҳларда кўринмай қолади,
> сатрлар ичида эса парсерни йиқитади. `tools/*.ps1` ни таҳрирлагандан кейин
> BOM жойидалигини текширинг.

## Архитектура

### Учта контекст ва улар орасидаги чегара

```
popup.js ──chrome.scripting.executeScript──> content/*.js
   │                                              │
   │  chrome.tabs.sendMessage                     │  chrome.runtime.sendMessage
   │  (UZ_RUN / UZ_REVERT / UZ_PING)              │  (UZ_TRANSLATE_BATCH / UZ_SUMMARIZE)
   ▼                                              ▼
saҳифадаги DOM ўзгариши                    background.js ──fetch──> openrouter.ai
```

Учта муҳим қоида шу схемадан келиб чиқади:

1. **API-калит ҳеч қачон content script'га тушмайди.** Барча `fetch` фақат
   `background.js` да. Саҳифадаги JS кенгайтма хотирасини ўқий олмайди, лекин
   калитни content script'га узатсак, у DOM контекстига яқинлашиб қолади.
   Шунингдек content script'дан қилинган `fetch` саҳифанинг CORS сиёсатига
   тушади - `host_permissions` фақат service worker'га тегишли.
2. **Content script'лар classic script, module эмас.** `executeScript({files})`
   уларни битта umumiy global scope'да кетма-кет бажаради, шунинг учун
   `translit.js`, `detect.js`, `extract.js` ўзини `self.UZ_*` га ёзади, кейин
   `content.js` шуларни ишлатади. **`popup.js` даги `CONTENT_FILES` тартиби
   муҳим** - `content.js` охирида туриши шарт.
3. **`background.js` - ES module** (`"type": "module"` манифестда), шунинг учун
   `src/shared/*.js` ни `import` қила олади. `options.js` ҳам module
   (`<script type="module">`), `popup.js` эса classic.

### Content script'лар вазифаси

- `extract.js` - Readability'нинг соддалаштирилгани. `pickRoot()` номзод
  блокларни матн узунлиги, `<p>/<li>` сони, ҳавола зичлиги ва
  `nav|footer|sidebar|comment|…` калит сўзлари бўйича баҳолаб, асосий
  контейнерни танлайди. `collectNodes()` ундан `TreeWalker` билан матн
  тугунларини йиғади, ҳар бирини `{node, raw, lead, core, trail}` кўринишида
  беради - `lead`/`trail` бўшлиқлари алмаштиришда сақланиши учун.
- `translit.js` - лотин→кирилл транслитерация. Кўп ҳарфли қоидалар
  (`o'`→`ў`, `sh`→`ш`, `ya`→`я`) узунлиги бўйича камайиш тартибида
  текширилади, сўз боши учун алоҳида жадвал бор (`e`→`э`). URL, e-mail ва
  `@`/`#` билан бошланувчи сўзлар тегилмайди.
- `detect.js` - тил тахмини, **фақат popup'даги маслаҳат учун**. Ҳеч қандай
  амални автоматик бошламайди.
- `content.js` - оркестратор: холат, оверлей (Shadow DOM ичида badge + хулоса
  панели), пакетлаш, параллеллик, аслига қайтариш.

### Таржима оқимидаги нозик жойлар

- **Аслига қайтариш** `originals = [{node, raw}]` рўйхатига таянади. Матнни
  алмаштиришдан **олдин** `rememberOriginal()` чақирилиши шарт, акс ҳолда
  «Аслига қайтариш» ишламай қолади.
- **Бўлаклар сони мос келиши шарт.** `translateSegments()` моделдан келган
  массив узунлигини текширади ва мос келмаса хато қайтаради - акс ҳолда матн
  нотўғри тугунларга тарқаб кетади. Промпт ҳам буни талаб қилади
  (`{"t": [...]}`, худди шу тартибда, худди шу сонда).
- **Кэш** `background.js` да, бўлак матни бўйича (`model + text`). Шунинг учун
  бир хил жумла иккинчи марта пул сарфламайди; кэш service worker уйқуга
  кетганда йўқолади, бу нормал.
- Бир нечта пакет хато берса иш тўхтамайди - `failures` йиғилади ва фақат
  ҳаммаси йиқилгандагина хато отилади.

### Промптлар

`src/shared/prompts.js` да, ҳаммаси инглиз тилида ёзилган (модель
кўрсатмаларни шундай яхшироқ бажаради), лекин **чиқиш тили - фақат кирилл
ўзбекча**. `CYRILLIC_RULES` да лотин/кирилл ўхшаш ҳарфлар тузоғи
(`a/а o/о p/р c/с e/е k/к x/х y/у`) ва «русча сўз ишлатма» қоидаси бор -
буларни олиб ташламанг, модель уларсиз аралаш матн чиқаради.

### Фреймлар

Кўп сайт (t.me, форумлар, embed'лар) асосий матнни **iframe** ичида кўрсатади.
Шунинг учун `popup.js` content script'ни `allFrames: true` билан инжекция
қилади ва ҳар бир фреймга `chrome.tabs.sendMessage(tabId, msg, {frameId})`
орқали алоҳида мурожаат қилади.

- **Ҳар фрейм ўз холатига эга.** Popup уларни `progressByFrame` да жамлайди;
  `chrome.runtime.onMessage` даги `sender.frameId` қайси фреймдан келганини
  айтади.
- **Юқори фрейм = `frameId 0`.** Хулоса панели ва badge доим ўша ерда
  кўрсатилади: iframe ичида `position: fixed` элемент iframe'нинг кичик
  виewport'ига қисилиб қолади.
- **Хулосани popup бошқаради**, content script эмас. `UZ_COLLECT_TEXT` энг кўп
  матнли фреймдан матн олади, popup уни background'га юборади, натижани эса
  `UZ_SHOW_TEXT` билан юқори фреймга беради. Шунинг учун `content.js` да
  `runSummary` йўқ.
- **Бошқа доменли iframe'лар** `activeTab` билан очилмайди. Улар учун
  `optional_host_permissions: ["<all_urls>"]` бор: popup'даги «рухсат бериш»
  тугмаси `chrome.permissions.request` ни чақиради. Бир хил доменли iframe
  (t.me холати) қўшимча рухсатсиз ишлайди.

### Кэш, бекор қилиш, белгиланган матн

- **Кэш икки қаватли** (`background.js`): хотирадаги `Map` (тез, service worker
  уйқуга кетса йўқолади) ва `chrome.storage.local` (сеанслар орасида яшайди).
  Калит = `SHA-256(модель + матн)` нинг биринчи 12 байти, префикси `tc:`.
  **Моделни калитга қўшиш шарт** - бошқа модель бошқача таржима қилади.
  Тозалаш FIFO: ўқилган вақт янгиланмайди, чунки ҳар ўқишда storage'га ёзиш
  кэшнинг маъносини йўқотган бўларди.
- **Бекор қилиш** иккита нарсани талаб қилади: `ctx.cancelled` байроғи янги
  пакетлар юборилишини тўхтатади, `UZ_ABORT` эса background'даги учиб турган
  `fetch`'ларни `AbortController` орқали узади. Иккови ҳам `runId` бўйича
  боғланган. Бекор қилинганда таржима қилиб улгурган матн жойида қолади -
  фойдаланувчи «Аслига қайтариш» билан ўзи қайтаради.
- **`AbortError` хато эмас.** `openrouter.js` уни `aborted: true` белгиси билан
  қайтаради, `content.js` эса уни `state.status = 'cancelled'` га айлантиради.
  Уни `failures` рўйхатига қўшманг.
- **Белгиланган матн** `Range` орқали ишлайди. `Range` `run()` бошида олинади:
  DOM ўзгаргандан кейин у бузилади. Қисман тегиб турган матн тугуни ҳам тўлиқ
  таржима қилинади - тугунни ярмидан кесиш грамматикани бузади.

## Конвенциялар

- **Узун тире (`—`) ишлатилмайди.** Лойиҳа файлларида (`.js`, `.md`, `.html`,
  `.css`, `.ps1`) фақат оддий дефис `-`. Бу фақат манба файлларга тегишли;
  таржима қилинадиган саҳифа матнига ҳеч қандай алоқаси йўқ.
- **Фойдаланувчи кўрадиган ҳамма матн - кирилл ўзбекчада.** Popup, options,
  оверлей, хато хабарлари, манифестдаги `name`/`description`. Кодда изоҳлар ҳам
  шу тилда; идентификаторлар инглизча.
- Созламалар калитлари битта жойда - `src/shared/config.js` даги `DEFAULTS`.
  `content.js` module эмаслиги учун ўзининг `CONTENT_DEFAULTS` нусхасини
  сақлайди; янги созлама қўшсангиз **иккаласини ҳам** янгиланг.
- Оверлей Shadow DOM ичида ва `[data-uz-overlay]` атрибутига эга -
  `extract.js` уни таржима қилинадиган матндан чиқариб ташлайди. Янги UI
  элемент қўшсангиз, у ҳам шу host ичида бўлсин.
- `chrome.storage.local` ишлатилади, `sync` эмас: API-калит қурилмалар орасида
  кўчирилмаслиги керак.
